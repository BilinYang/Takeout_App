<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bilin.mapper.SetMealMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal values (null, #{categoryId}, #{name}, #{price}, #{status}, #{description},
                   #{image}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <select id="countByCategoryId" resultType="java.lang.Integer">
        select count(*) from setmeal where category_id=#{categoryId}
    </select>

    <select id="getById" resultType="com.bilin.entity.SetMeal">
        select * from setmeal where id=#{id}
    </select>

    <update id="update">
        update setmeal
        <set>
            <if test="#{categoryId}!=null">
                category_id = #{categoryId},
            </if>
            <if test="#{name}!=null and #{name}!=''">
                name = #{name},
            </if>
            <if test="#{description}!=null">
                description = #{description},
            </if>
            <if test="#{price}!=null">
                price = #{price},
            </if>
            <if test="#{status}!=null">
                status = #{status},
            </if>
            <if test="#{image}!=null">
                image = #{image},
            </if>
            <if test="#{createTime}!=null">
                create_time = #{createTime},
            </if>
            <if test="#{updateTime}!=null">
                update_time = #{updateTime},
            </if>
            <if test="#{createUser}!=null">
                create_user = #{createUser},
            </if>
            <if test="#{updateUser}!=null">
                update_user = #{updateUser},
            </if>
        </set>
        where id=#{id}
    </update>

    <select id="pageQuery" resultType="com.bilin.entity.SetMeal">
        select * from setmeal
            <where>
                <if test="name!=null and name!=''">
                    name like concat('%', #{name}, '%')
                </if>
                <if test="categoryId!=null">
                    and category_id = #{categoryId}
                </if>
                <if test="status!=null">
                    and status = #{status}
                </if>
            </where>
        order by update_time desc
    </select>

    <delete id="deleteBatch">
        delete from setmeal where id in 
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
    </delete>

    <update id="enableDisable">
        update setmeal set status=#{status}
        where id=#{id}
    </update>

    <select id="list" parameterType="Setmeal" resultType="Setmeal">
        select * from setmeal
        <where>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from setmeal
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
        </where>
    </select>

</mapper>
